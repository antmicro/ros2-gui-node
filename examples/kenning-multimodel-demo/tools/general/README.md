# General tools used for demo initialization and execution

Directory which stores scripts for demo initialization and execution compatible with multiple Linux platform:

* `init-demo.sh` - It is a script that initializes demo directory with necessary repositories
* `run-demo.sh` - It is a script used to start demos
* `download-resources.sh` - It is a script used for to download resources necessary for running the demo
